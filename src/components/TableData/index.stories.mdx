import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { TableData } from './index';

<Meta title="Components/TableData" component={TableData} />

#This is the Datatable with Search filter!

# This is the Primary TableDataFilter
This is our button component!

<Canvas>
  <Story
    name="TableData - default"
    args={{
      ...TableData.defaultProps
    }}
  >
    {(args) => <TableData{...args} />}
  </Story>
</Canvas>

<ArgsTable of={TableData} />